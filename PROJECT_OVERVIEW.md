# OSINT 社交情报平台 — 项目说明

---

## 一、这个项目是做什么的？

一套自动化社交情报采集系统。简单说：**用 AI 伪装成真人，在 Telegram / Facebook / Zalo 上和目标对象聊天，自动收集有价值的情报信息。**

### 具体能做什么？

**主动寻找目标**
- 输入关键词（如"换汇""USDT""私家侦探"），系统自动在 Telegram 上搜索相关群组
- AI 会智能扩展关键词：比如搜"虚拟币"，会自动联想出"crypto""OTC""场外交易"等相关词一起搜，覆盖面更广
- 搜索结果展示群组名称、人数、简介，可一键加入
- 也支持直接粘贴群组链接或用户名添加
- Facebook 和 Zalo 的群组搜索功能也在开发中

**自动渗透聊天**
- 加入群组后，AI 账号像普通群友一样参与讨论
- 也可以主动私聊群内成员，发起一对一对话
- 每个 AI 账号有完整的人设：名字、年龄、职业、所在城市、个人经历，对话风格自然真实
- 支持多语言：主要用越南语，对方用中文或英文时自动切换
- 可同时运行多个账号，各自独立人设、独立聊天，互不影响

**智能对话采集**
- AI 不是机械问答，而是分阶段推进对话：先打招呼建立信任 → 自然闲聊试探 → 发现业务信号后深入询问联系方式、价格、服务范围等 → 信息够了礼貌结束
- 如果对方表现出警惕（比如说"你是机器人吧""骗子"），AI 会自动道歉并安全退出，不会纠缠
- 新用户首次联系需通过一道简单算术题验证，防止对方也是机器人浪费资源
- 每条消息都有模拟打字延迟，回复节奏像真人

**情报自动提取**
- 从对话内容中自动识别并提取：手机号、Zalo号、Telegram用户名、网站链接、Facebook主页、价格信息、地址、银行账号等
- 按业务类别自动归档：私家侦探 / 换汇服务 / 自由职业外包 / 数据贩卖
- 每条情报带置信度评分，低置信度的可以人工复核
- 自动去重：同一个人的同一条信息不会重复录入
- 所有情报可在后台按类别、平台、时间筛选查看

**全程可控可监控**
- 管理后台实时显示所有对话内容，可随时查看任意账号的聊天记录
- 每个账号可单独设置回复策略：只回私聊、不回群组、不回机器人消息等
- 可随时拉黑某个用户，被拉黑后所有账号都不再回复此人
- 账号健康状态一目了然（正常/警告/危险/封禁），出问题第一时间知道
- 所有操作都有审计日志，谁做了什么、什么时候做的，全部可追溯

---

## 二、系统怎么运作的？

### 整体流程（4步）

```
① 准备账号 → ② 找到目标 → ③ AI 自动聊天 → ④ 情报入库
```

**① 准备账号**
- 导入多个社交平台账号（Telegram / Facebook / Zalo）
- 每个账号有独立的"养号"策略，模拟真人使用习惯，避免被封
- 可设置每个账号回复哪些消息（私聊/群组/频道/机器人）

**② 找到目标**
- 手动添加群组或通过关键词搜索
- 支持 AI 智能扩展关键词（比如搜"虚拟币"会自动联想"crypto""USDT"等）
- 一键加入目标群组

**③ AI 自动聊天**
- AI 扮演预设角色（有名字、年龄、职业、背景故事）
- 对话分阶段推进：打招呼 → 闲聊试探 → 深入采集 → 礼貌结束
- 遇到可疑用户（比如说"你是机器人吧"）会自动安全退出
- 新用户需通过算术验证，防止对方也是机器人
- 每条消息都有打字延迟，看起来像真人在打字

**④ 情报入库**
- 自动从对话中提取：手机号、Zalo号、Telegram号、网站、价格、地址等
- 按类别归档：私家侦探 / 换汇 / 自由职业 / 数据贩卖
- 去重处理，同一条情报不会重复录入
- 可在后台随时查看、审核、标记

---

## 三、AI 对话是怎么设计的？

### 对话阶段

| 阶段 | 做什么 | 什么时候切换 |
|------|--------|-------------|
| 验证 | 出算术题确认对方是真人 | 答对后进入下一阶段 |
| 打招呼 | 友好问候，建立信任 | 1-2轮后 |
| 试探 | 自然地聊相关话题 | 发现业务信号词后 |
| 采集 | 深入询问联系方式、价格等 | 信息够了或超过15轮 |
| 转向 | 换个角度继续聊 | 当前话题没进展时 |
| 结束 | 礼貌告别 | 采集完成或需要退出 |
| 冷却 | 道歉并离开 | 对方表现出警惕 |

### 关于上下文记忆

目前每次对话 AI 能"记住"最近约 20 条消息 + 一份简短摘要。

**升级计划：将上下文容量提升到约 10K tokens（约7000-8000个中文字）**，具体做法：
- 把对话历史从 20 条扩展到 50 条
- 让摘要更详细（从200字扩展到500字）
- 把之前收集到的该用户情报也喂给 AI，避免重复提问

这样 AI 就能在更长的对话中保持一致性，记住更多细节，采集效率更高。

---

## 四、安全保障

### 账号保护
- **养号机制**：新号前7天每天只发20条消息，逐步增加到200条，模拟真人成长曲线
- **速率限制**：严格控制每小时/每天的消息量，超限自动暂停
- **行为模拟**：打字速度、阅读停顿、操作间隔都模仿真人节奏
- **活跃时段**：只在越南时间 8:00-23:00 活动

### 反检测
- 每个账号绑定固定代理IP，不频繁切换
- 设备信息伪装（型号、系统版本、App版本）
- 对话风格有人味：偶尔打错字、用表情、语气自然

### 数据安全
- 所有对话记录存入数据库，可追溯审计
- 操作日志完整记录
- 敏感凭证仅存本地，不入代码库

---

## 五、管理后台功能

| 模块 | 功能 |
|------|------|
| 账号管理 | 添加/删除账号、查看健康状态、自定义显示名、设置回复策略 |
| 群组管理 | 搜索群组、AI优化关键词、一键加入 |
| 对话监控 | 实时查看所有对话、历史消息回放、拉黑用户 |
| 情报告报 | 按类别/平台筛选、置信度评分、审核状态 |
| 任务管理 | 创建采集任务、指定平台和关键词、启动/停止 |
| 服务控制 | 启停各平台聊天服务、查看运行日志 |

---

## 六、当前进度

| 模块 | 状态 |
|------|------|
| Telegram 自动聊天 | ✅ 已上线运行 |
| 账号管理 & 养号 | ✅ 已完成 |
| 群组搜索 & 加入 | ✅ 已完成 |
| 对话监控 & 实时推送 | ✅ 已完成 |
| 情报提取 & 入库 | ✅ 已完成 |
| 回复策略控制 | ✅ 已完成 |
| Facebook 适配器 | ⏳ 代码就绪，待实测 |
| Zalo 适配器 | ⏳ 代码就绪，待实测 |
| 10K 上下文升级 | 📋 方案已定，待实施 |

---

## 七、部署方式

一台服务器即可运行全部服务：

- 后端 API + AI 对话引擎
- 任务调度器
- 前端管理界面
- 数据库 + 缓存

支持 Docker 一键部署，也支持本地直接运行。
